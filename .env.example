# Local Express server (npm run server)
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/xpverse
JWT_SECRET=your-long-random-access-secret
JWT_REFRESH_SECRET=your-long-random-refresh-secret

# Optional: override API URL in the Vite app (leave unset on Vercel to use /api)
# VITE_API_URL=http://localhost:3001/api
